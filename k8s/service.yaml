apiVersion: v1
kind: Service

metadata:
  name: employee-service
  namespace: employee-app

spec:
  type: NodePort

  selector:
    app: employee-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8089
      nodePort: 30089